# 2026-08-09

## 新增 AGENTS.md（博客发布 + Git 操作规范）

- 在仓库根目录创建 `AGENTS.md`，作为后续"发布博客"任务的唯一执行手册。
- 内容来源：调研本仓库结构（Jekyll 4.3 + chirpy 7.2、212 篇 `_posts`、三类固定栏目、assets/radar、html/ 目录、pages-deploy 工作流）+ 吸收 `~/.catpaw/skills/github-git-ops/SKILL.md` 的 commit 规范。
- 关键约定已固化：
  - 发布流程 Step0 必须先 `git pull`（仓库每天有自动化任务写入，不 pull 必冲突）
  - 文章路径 `_posts/YYYY-MM-DD-slug.md`，front matter `date` 必带 `+0800`
  - 三类固定栏目标题格式：日报 `Latte 热点报告 · MMDD`、雷达 `AI 全域雷达报告 · YYYY-MM-DD`、热度榜 `GitHub AI Agent 项目综合热度榜 - YYYY-MM-DD`
  - commit message：`<type>(<scope>): <中文subject>`，type 限定 func/fix/refactor/impr/perf/apm/chore/conf/docs/style/test/typo/wip，不能含换行
- 同步在 `_config.yml` 的 `exclude` 中加入 `AGENTS.md`，避免被 Jekyll 渲染成站点页面。
- 安全处理：AGENTS.md **不写入 GitHub Token**（这是公开仓库），只指向本地 `~/.catpaw/skills/github-git-ops/SKILL.md` 读取。
- 提交：`729060a docs(*): 新增 AGENTS.md 博客发布与 Git 提交操作规范; _config.yml 排除该文件不参与站点渲染`，已推送 main。

## 待办 / 风险

- 用户的 GitHub PAT `ghp_eMhh...`（明文存在本地 skill 文件里）已在多轮对话中出现，建议轮换并改用 keychain / gh auth 存储。
